// lib/core/enums/game_enums.dart
enum ParticleType { food, powerUp, collision, trail }
enum GameState { playing, paused, gameOver, completed }
enum Direction { up, down, left, right }
enum PowerUpType { speedBoost, shield, slowMotion, ghost, doubleScore } // Your 4 focused power-ups
enum Difficulty { easy, normal, hard }